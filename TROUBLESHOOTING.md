# 安装指引与问题排查（写给没有技术背景的朋友）

本手册假设你**从来没碰过命令行**，每一步都告诉你「打开什么、点什么、输入什么」。照着做就行，全程大约 15 分钟（大部分时间在等下载）。

---

## 0. 开始前的自查（3 件事）

| 检查 | 怎么查 | 不满足怎么办 |
|---|---|---|
| 电脑是 Windows 10/11 | 看系统设置 → 关于 | 本包目前只支持 Windows |
| ZCode 已安装**并且至少聊过一次** | 打开 ZCode 能正常对话 | 先装 ZCode 并随便聊一轮再回来 |
| 磁盘有 1GB 以上空闲 | 此电脑里看 C 盘 | 清理后再装 |

> 你不需要管理员权限，不需要会编程，不需要科学上网（模型下载走国内镜像）。

---

## 1. 安装 Python（已装过跳到第 2 步）

**先判断有没有装过**：按 `Win + R`，输入 `cmd` 回车，在黑窗口里输入：

```
python --version
```

- 显示 `Python 3.10` 到 `3.13` 之间的版本号 → 已装好，去第 2 步
- 显示 `不是内部或外部命令`，或弹出了 Microsoft Store → 没装好，按下面装

**安装步骤**：

1. 打开 https://www.python.org/downloads/ ，点黄色的 **Download Python 3.x.x** 按钮
2. 双击下载的安装包。**第一个界面最底下有一个小勾选框「Add Python to PATH」，务必先勾上它**（90% 的安装失败都是漏了这一勾）
3. 点 **Install Now**，等进度条走完，点 Close
4. **关掉刚才的黑窗口，重新开一个**（按 `Win + R` → `cmd` → 回车），再输入 `python --version` 验证能显示版本号

> 装的时候没勾「Add Python to PATH」？最简单的办法：卸载 Python（设置 → 应用 → 搜 Python → 卸载），重新装一遍并勾上。

---

## 2. 拿到安装包

**方式 A（推荐，最简单）：网页下载 zip**

1. 让分享者把你加进仓库后，打开仓库页面 → 绿色 **Code** 按钮 → **Download ZIP**
2. 把下载的 zip **解压**到一个简单的位置，比如 `D:\paios-kit`（不要放在桌面或带中文的文件夹里，路径越简单越不出幺蛾子）

**方式 B（会用 git 的话）**：

```
git clone https://github.com/TonyQ-AI/paios-kit.git D:\paios-kit
```

---

## 3. 安装（双击一下就行）

打开解压后的文件夹，**双击 `install.bat`**。会弹出一个黑窗口滚动安装信息，等它显示 `=== done ===` 并停下来（约 2~5 分钟，视网速）。

想自定义安装位置或端口的话，不用双击，改用命令行（`Win + R` → `cmd`）：

```
cd /d D:\paios-kit
python install.py --root D:\tools\paios --port 8743
```

**安装器会自动处理的事情**（你不用管）：装依赖库、初始化数据、把功能注册进 ZCode、端口被占自动换、已有配置只合并不覆盖。

---

## 4. 装完后的 3 个动作

1. **完全退出 ZCode 再打开**（注意右下角托盘图标也要退出，不是只关窗口）
2. 打开安装文件夹（默认在 `C:\Users\你的用户名\paios`），**双击 `launch_ui.vbs`**——浏览器会打开记忆库页面
3. 在 ZCode 里随便发一句话试试，比如：

   > 搜一下我的历史会话

   AI 能列出来，就说明全部通了。

---

## 5. 需要你手动配置的东西（只有 2 项，且都是可选）

| 配置 | 不配会怎样 | 怎么配 |
|---|---|---|
| ① 项目目录（workspace-roots） | 会话照样归档，只是「属于哪个项目」的判定偶尔不准，会多归到「未归类」 | 见下方命令 |
| ② 代理（你平时上 GitHub 需要代理时才配） | 知识库云备份推不出去（本机数据不受影响） | 见下方命令 |

**配置 ①**（把 `D:\我的项目` 换成你实际放项目的文件夹，可以有多个用逗号隔开）：

```
cd /d D:\paios-kit
python install.py --workspace-roots "D:\我的项目"
```

**配置 ②：经验库云备份**（可选——想给自己的经验库留一份云端备份才需要，不配一切正常）

看不懂这段？说明你不需要它，直接跳过，不影响任何功能。

1. 先在 GitHub 建一个**私有**（Private）空仓库（不要选公开），复制它的地址；
2. 打开命令行（`Win + R` → 输入 `cmd`），依次执行（安装目录默认是 `C:\Users\你\paios`）：

   ```
   cd /d C:\Users\你\paios
   git init -b main
   git remote add origin 你刚复制的仓库地址
   setx PAIOS_GIT_PROXY "http://127.0.0.1:7890"
   ```

   最后一行只有「你上 GitHub 需要代理」时才执行，`7890` 换成你自己代理软件的端口
   （在你代理软件的设置里能看到）；不需要代理就整行跳过，它会直连。
   （老版本 Git 不支持 `init -b`，改用 `git init` 后执行 `git checkout -b main`）

3. **完全退出 ZCode（含右下角托盘图标）再重新打开**——注意：不是重开命令行窗口，
   一定要重开 ZCode 本身，配置才会在会话结束时生效。

配好后每次会话结束会自动提交并推送经验库；界面右上角不再出现「⚠ 推送失败」即为正常。
想手动验证一次：命令行里 `cd /d 安装目录` 后执行 `git log --oneline -3`，能看到
`atoms: auto-sync ...` 的提交就说明链路通了。

懒人版：把下面这段发给你的 ZCode 让 AI 代做——

```
帮我给安装根 <安装目录> 配置经验库云备份：git init -b main、加远程 <仓库地址>、
（需要代理的话）设置 PAIOS_GIT_PROXY=<我的代理地址>，完成后告诉我怎么验证。
```

---

## 6. 常见问题对照表（症状 → 解决）

**「python 不是内部或外部命令」**
→ Python 没装或没勾 PATH。回到第 1 步重装，第一屏勾选「Add Python to PATH」。

**双击 install.bat 窗口一闪就没了**
→ 说明安装报错退出了。手动看报错：`Win + R` → `cmd` → 输入 `cd /d D:\paios-kit`（换成你的解压目录）→ 输入 `python install.py`，把最后几行报错对照本表，或发给分享者。

**pip 安装依赖很慢 / 报红字超时**
→ 安装器已自动切换国内清华镜像重试。如果还失败，手动执行：
`python -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`

**提示模型下载失败 / semantic 相关报错**
→ 模型（约 90MB）从国内镜像自动下载，偶尔网络波动失败。**不影响其他功能**（检索、归档都正常，只是「语义查重」暂缺），下次运行会自动重试。着急的话换一个网络环境重跑 `python install.py`。

**装完后 ZCode 里 AI 说没有 paios 工具**
→ 90% 是没重启干净：右下角托盘里的 ZCode 也要右键退出，然后重新打开。还不行：打开 ZCode 设置 → MCP，看 `paios` 是否显示已连接（红色则点开看报错信息）。

**记忆基座界面右上角出现「⚠ 推送失败(日期)」**
→ 这是「经验库云备份」没推上去的提示，**本机数据完全不受影响**（你的会话记录和经验
原子都好好存在本机）。常见原因：① 你上 GitHub 需要代理但没配 → 见第 5 节配置②；
② 配了但代理软件没开、或端口填错了。
→ 修好后**必须完全退出 ZCode（含右下角托盘图标）再重新打开**，推送会在下次结束会话时
自动补上（不用手动重推）。提示消失即为恢复。

**双击 launch_ui.vbs 浏览器没弹出来**
→ 先手动开浏览器访问 `http://127.0.0.1:8643`，能打开就说明服务正常（只是浏览器没自动弹）。打不开就双击 `install.bat` 重跑一遍（幂等，不会弄坏东西）。

**杀毒软件/Windows Defender 报警拦截**
→ 本套件是本地 Python 脚本，只在下载模型时联网。选择「允许」并把安装目录加入白名单。

**360 等安全卫士弹「某程序正在修改浏览器/注册表」**
→ 安装过程不碰注册表和浏览器设置，弹窗一律选「允许」即可（是脚本在写 ZCode 的配置文件触发的误报）。

**想把系统挪到别的盘 / 换端口**
→ `cd /d 解压目录` 后执行 `python install.py --root D:\新位置 --port 8743`。数据不会自动搬家，换位置后把旧安装目录里的 `data\` 和 `knowledge\` 两个文件夹拷到新位置即可。

**用了一段时间想换电脑**
→ 拷贝旧机安装目录下的 `data\` 和 `knowledge\` 两个文件夹 → 新机装好本套件后把这两个文件夹覆盖到新安装目录 → 完成。

**想彻底删除**
→ 双击 `uninstall.bat`（摘除 ZCode 里的注册项），再手动删除两个文件夹：解压目录、安装目录（默认 `C:\Users\你\paios`，你的会话记录和经验库在里面，删了就没了）。

---

## 7. 装好之后怎么用（跟 AI 直接说话就行）

- 「搜一下我的历史会话」——查你让 AI 干过什么
- 「这个报错以前踩过吗」——AI 会自动查你的踩坑经验库
- 「沉淀一下这个经验」——把刚修好的坑记入经验库（AI 会先征求你确认）
- 「新项目立项」——按你的流程开一个新项目（会话大脑 + 规格环境一步到位）
- 每次会话结束时系统自动记账，不需要你做任何事

有本表没覆盖的问题：把黑窗口里的报错最后几行截图发给分享者。
