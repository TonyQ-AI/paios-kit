# PAIOS 个人 AI 作业系统（记忆基座 + 知识层，全局可用）

工具主目录 `{{PAIOS_ROOT}}`（已注册 MCP server `paios`，工具全部 `paios_` 前缀）。

- 记忆基座 UI：{{UI_URL}} （会话归档 / 项目总览 / 知识原子 / 候选沉淀）

## 检索规则（读取端，每次任务开工执行）
- 任务开工时，从任务描述提取 2~3 个关键词，调 `paios_search_atoms` 检索经验原子库；**命中才读对应原子（避旧坑、守约束），未命中不注入任何内容**。
- **精读纪律（省 token）**：检索结果按相关性排序，每条自带结论+边界——**两者合起来已够行动就以此为准**（边界=何时不适用，照结论做之前先扫一眼边界防误用）；需要操作细节/背景时才读全文，最多精读 1~2 条。严禁把命中列表整批读入。
- **踩坑确诊**：遇到诡异报错/异常行为时，提取报错特征词（异常名/症状/组件名）调 `paios_search_atoms` 查有没有踩过同款——原子标题都是症状式的，命中即得现成解法；未命中再走系统化排查，修完按沉淀规则提议入库。
- 需要查历史会话时用 `paios_search_sessions` / `paios_get_session`；提炼素材用 `paios_get_session_md` 读原文。

## 沉淀触发（写入端）
- **只提议不越权两条线（红线）**：
  1. **写入（新建原子）**：AI 只在「确定能救命 / 跨项目可复用」时才**提议**；普通细碎经验不提。**用户点头才写入**（连草稿都不先写）——这条不松。
  2. **转正（draft→active）**：按下方授权规则批量执行 + 用户抽查 + 可回滚。
- **转正授权规则**（AI 按此批量执行，用户抽查，转错随时废弃恢复）：
  - 满足「独立问题 + 有明确技术层/场景边界 + 跨项目可复用」的草稿 → 主动转正
  - **「跨项目可复用」按「可预见会再遇到」判定**：抽象通用但预见不到再用场景的——一次性领域情报（如某平台数据源调研）、做完即止的工具配方——一律废弃，不转正
  - 同族但属不同技术层（如 bash 转义 vs JS 转义）→ 各自独立转正，**不并入**
  - 同质重复 → 并入已有 active 原子，不新建
  - 纯项目私有 / 无跨项目价值 → 废弃（标 deprecated）
  - 转正后告知用户清单供抽查；任何转正可用 deprecated→active 一键回滚
- **价值门槛（防垃圾场，写入门槛的核心）**：只沉淀「换一个项目或半年后还用得上」的经验；纯项目内部细节、一次性调试痕迹、可在项目 BLUEPRINT 里写清的不入原子库。
- **项目收尾**（交付/大版本完成）→ 必须主动提议一次「沉淀盘点」：扫 `paios_list_candidates` + 回顾项目全程，列出值得沉淀的经验清单（方案/决策/工作流/约束，不只踩坑）供用户勾选。
- 归属判断筛子：**换项目或半年后还用得上 → 原子库；只属于项目本身 → 项目 BLUEPRINT/README**；进度类 → 项目任务清单/登记卡。

## 项目登记卡 My_NOTE.md（仅项目开发会话生效）
**适用范围：只服务「项目开发会话」**——在项目目录里新建/修改功能代码、产出项目文件的工作。纯配置类会话（改全局/机器配置、环境修复、凭证调整、一次性调试等不落项目产物、不建项目目录的任务）**不创建登记卡**，会话结束即走。
项目开发会话开始前，先检查项目根目录是否存在 `My_NOTE.md`：
1. 不存在 → 先创建，按下方模板填写（先填占位，任务中持续完善）：
   # 项目名
   - 用途：<这个项目做什么>
   - 工具：<本次使用的 AI 工具，如 zcode / codex>
   - 会话：<本次会话标题或标识>
   - 会话路径：<会话数据所在路径：zcode=~/.zcode 会话；codex=~/.codex/sessions>
   - 项目路径：<项目所在绝对路径>
   - 创建时间：<YYYY-MM-DD HH:MM>
   - 更新时间：<YYYY-MM-DD HH:MM>
   - 状态：进行中
   - 标签：<标签1, 标签2>
   - 产物目录：<产物相对或绝对路径>
   ## 说明
   <背景、功能、关键文件、注意事项>
2. 存在 → 任务开始或结束时，更新其中的 用途 / 状态 / 更新时间 / 产物目录 / 会话 字段。
要求：保持 ≤40 行，只记稳定的项目事实，不记对话流水。
