# 新项目启动规则
**适用范围：仅限「启动全新项目」这一时刻（从零创建一个新仓库/新工程时）生效。** 已有项目的日常开发、功能迭代、修 bug、重构等一律不触发本规则，直接按项目现状处理。
新项目启动时，必须先做开源社区调研，禁止直接动手写代码：

1. 根据需求提炼关键词，在 GitHub、Gitee、GitLab、SourceForge 等开源社区搜索有没有高完成度的类似项目可以参考
   - 用代码/仓库搜索、Web 搜索等渠道，优先找：高 star、活跃维护、功能覆盖度≥需求 80% 的项目
2. 找到候选项目后做快速评估：star/fork 数、最近提交时间、license、技术栈、文档完整度，输出一份简短调研结论（候选项目 + 可复用点/差距/推荐决策：直接复用 / 基于其二次开发 / 仍从零开发并说明理由）
3. 调研结论放入项目初始说明或计划中，再进行下一步开发
4. 除非用户明确要求从零实现（学习目的、许可冲突、需求差异过大等），否则优先复用或参考已有项目
5. **立项初始化（确定为新建仓库/工程后、动手写代码前）**：用 `$new-project` skill 一键生成项目级 `AGENTS.md` 会话大脑（生产底线/当前功能与状态/决策记录/续接点）+ `My_NOTE.md` 登记卡，并初始化 SpecKit-CN 规格环境（`.specify/`），让项目从第一天起就带生产底线 + 跨会话续接，不走「先 MVP 跑通再重构」路线。若调研决定「直接复用现有项目」（而非新建），视为已有项目日常开发，不触发本次初始化，按项目现状处理。
